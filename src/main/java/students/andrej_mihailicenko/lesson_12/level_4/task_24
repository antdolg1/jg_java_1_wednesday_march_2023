- ArrayIndexOutOfBoundsException - Вызывается при попытке добавить ячейку в массив с некорректным индексом (негативным
или большим чем массив)

- IllegalArgumentException - Вызывается, чтобы указать, что методу был передан недопустимый аргумент.

- IOException - Вызывается при неудачных/прерванных операциях ввода-вывода

- StackOverflowError - Возникает при переполнении стека из-за слишком глубокой рекурсии приложения.
метод вызывает сам себя (или другие методы), создавая цепочку вызовов, которая не завершается.
По мере каждого нового вызова метода, информация о вызове помещается в стек, чтобы затем вернуться к предыдущему
вызывающему методу.

- OutOfMemoryError - Генерируется, когда виртуальная машина Java не может выделить объект из-за нехватки памяти

- NumberFormatException - возникает, когда метод, ожидающий числовое значение,
получает строку, которую невозможно правильно интерпретировать как число.