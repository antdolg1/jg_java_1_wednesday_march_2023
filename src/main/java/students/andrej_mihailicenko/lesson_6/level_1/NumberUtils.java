package students.andrej_mihailicenko.lesson_6.level_1;

class NumberUtils {
    public boolean isEven(int number) {
        return number % 2 == 0 ? true : false; // Использован тернарный оператор. Если number %2==0 true то выполняется return true.
    }

}